\ProvidesPackage{decade}

\RequirePackage{listings}

\lstdefinelanguage{lustre}{
morekeywords={
node,returns,var,allocate,mutable,let,tel,const,pre,last,fby,
merge,when,at,if,then,else},
otherkeywords={=, [, ], :, ;, <<, >>, <-},
sensitive=true,
morecomment=[n]{(*}{*)},
morestring=[b]",
morestring=[b]'
}

\lstdefinelanguage{decade}{
morekeywords={
inline, async,fun,future,node,returns,var,let,tel,const,pre,last,do,reset,every,fby,at,
type,merge,when,whenot,not,automaton,end,switch,case,state,until,if,then,else,
unsafe,map,with,default,mapfold,fold,new,int,float,bool,init,fun,split,fifo, on},
otherkeywords={->},
sensitive=true,
morecomment=[n]{(*}{*)},
morestring=[b]",
morestring=[b]'
}

\lstdefinelanguage{obc}{
morekeywords={
machine,registers,future,vars,objects,instances,stocks,reset,switch,step,float,int,var,case,mutable,for,to},
otherkeywords={},
sensitive=true,
morecomment=[n]{(*}{*)},
morestring=[b]",
morestring=[b]'
}

\lstdefinelanguage{signal}{
morekeywords={process,when,default,where,init,end,not,boolean,integer,event},
otherkeywords={?,!},
sensitive=true
}

\lstset{language=decade}
