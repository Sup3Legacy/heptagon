\ProvidesPackage{decade}

\RequirePackage{listings}

\lstdefinelanguage{lustre}{
morekeywords={
node,returns,var,allocate,mutable,let,tel,const,pre,last,fby,
merge,when,at,if,then,else},
otherkeywords={=, [, ], :, ;, <<, >>, <-},
sensitive=true,
morecomment=[n]{(*}{*)},
morestring=[b]",
morestring=[b]'
}

\lstdefinelanguage{decade}{
morekeywords={
async,fun,future,node,returns,var,let,tel,const,pre,last,do,reset, every,fby,at,
type,merge,when,whenot,not,automaton,end,switch,case,state,until,if,then,else,
map,with,default,mapfold,fold,new,int,float,bool,init,fun,split},
otherkeywords={->},
sensitive=true,
morecomment=[n]{(*}{*)},
morestring=[b]",
morestring=[b]'
}

\lstdefinelanguage{obc}{
morekeywords={
machine,registers,instances,switch,step,float,int,var,case,mutable,for,to},
otherkeywords={},
sensitive=true,
morecomment=[n]{(*}{*)},
morestring=[b]",
morestring=[b]'
}


\lstset{language=decade}
