
(* Second test of model node - more periods *)
model main(a:int; b:int) returns (c:int)
var
	d : int :: [0,2];
	e : int :: [2,6];
let
	d = a when [0,2];
	e = d when [1,3];

(*	c = b + current([0,2], 0, current([1,3], 0, e)); *) (* Alternate equation for c *)
	c = b + current([2,6], 0, e);

tel

