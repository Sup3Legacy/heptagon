
(* Second test of undefined phase - exemple from EMSOFT article *)

model main(a:int) returns (e:int)
var
	b : int :: [..,2];
	c : int :: [..,6];
	d : int :: [..,2];
let
	b = buffer (a when [1,2]);
	c = buffer (b when [0,3]);
	d = buffer current([2,3], 0, (buffer c));
	e = buffer current([1,2], 0, (buffer d));
tel