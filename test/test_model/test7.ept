
(* First test of undefined phase with annotations *)

model main(a:int) returns (c:int)
var
	b : int :: [..,4];
	b1 : int :: [..,4];
let
	label(eqb) b = buffer (a when [1,4]);
	label(eqb1) b1 = buffer b;
	c = current([3,4], 0, b1);
tel
requiring
	before(eqb, eqb1)
end
