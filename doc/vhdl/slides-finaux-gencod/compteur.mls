node compteur(tick : bool; top : bool) returns (o : int)
var pres : int;
let
  o = ( + )(if top then 0 else 0 fby o, pres);
  pres = if tick then 1 else 0
tel

node main returns (o : int)
let
  o =
    compteur
    (true fby true fby false fby true, false fby false fby true fby false)
tel

